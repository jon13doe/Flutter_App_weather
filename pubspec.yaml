name: current_location_weather
description: "Weather app"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.1.0
  weather: ^3.1.1
  intl: ^0.19.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  home_widget: ^0.4.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/